---
name: Create Boutiques descriptor

on:
  workflow_dispatch:

jobs:

  create-descriptor:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v6

    - uses: actions/setup-python@v6

    - name: Install package
      run: |
        pip install -U pip
        pip install .

    - name: Create descriptor
      uses: michellewang/argparse-to-boutiques@b0febae0cc19d64d120a7884275f9e650f574453
      with:
        parser-location: my_tool.cli:get_parser
        output-path: my_tool/data/descriptor.json
