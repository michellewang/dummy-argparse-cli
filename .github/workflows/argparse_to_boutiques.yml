---
name: Create Boutiques descriptor

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:

  create-descriptor:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Set up Python
      uses: actions/setup-python@v6

    - name: Install package
      run: |
        pip install -U pip
        pip install .

    - name: Create descriptor
      uses: michellewang/argparse-to-boutiques@9228d9ee642cefc14931fdd15ae9a26ce7ed9178
      with:
        parser-location: my_tool.cli:get_parser
        output-path: my_tool/data/descriptor.json
        token: ${{ secrets.PAT }}
